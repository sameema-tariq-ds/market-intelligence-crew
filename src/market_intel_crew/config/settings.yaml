app_env: "development"

app:
  app_name: "MarketIntelCrew"

paths:
  logs: "logs"
  output: "output"
  crewai_storage: ".crewai_storage"

llm:
  provider: "cerebras"          # cerebras | openai | ollama
  model: "llama3.1-8b"
  max_tokens: 2048
  temperature: 0.3

agents:
  research_max_iter: 2

logging:
  level_development: "DEBUG"
  level_production: "INFO"
  format: "pretty"              # pretty | json
